"use strict"

module.exports = function renderLinks(root, place) {
  place.actions.forEach(function(action) {
    action.href = renderLink(root, action.href)
  })
  return place
}

function renderLink(root, link) {
  // needed here to resolve: url of self, other ids and their url
  return root + link
}
